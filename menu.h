int getMenu(const char* menuHeader, const char** menuSet, int numOfHeaders);
/* ruft ein Menue mit einer Ueberschrift (Header), einer Liste von Menuepunkten (menuSet) und einer Anzahl Menuepunkte (numOfHeaders) auf.
    gibt Anzahl Optionen zurueck. */
